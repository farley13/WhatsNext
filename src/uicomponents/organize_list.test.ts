



test('Add calls back to datasource', () => {
  expect(1 + 2).toBe(3);
});

test('Remove calls back to datasource', () => {
  expect(1 + 2).toBe(3);
});

test('Quick Edit sends updated values to datasource', () => {
  expect(1 + 2).toBe(3);
});
