

test('Edit loads correct datasource values', () => {
  expect(1 + 2).toBe(3);
});

test('Edit loads and displays enum values', () => {
  expect(1 + 2).toBe(3);
});

test('Edit save sends updated values to datasource', () => {
  expect(1 + 2).toBe(3);
});

test('Edit cancel does not impact datasource', () => {
  expect(1 + 2).toBe(3);
});
